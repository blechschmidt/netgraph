# E045 (NG-S012) - group membership closes a loop.
#
# 'everyone' holds 'engineering', which holds 'everyone' back. Neither group has
# a membership that can be listed: expanding either one does not terminate, so
# no consumer of this inventory -- netgraph, a directory, or a reader -- can
# answer "who is in this?".
apiVersion: netgraph.dev/v1alpha1
kind: group
metadata:
  name: everyone
spec:
  members:
    - engineering
---
apiVersion: netgraph.dev/v1alpha1
kind: group
metadata:
  name: engineering
spec:
  members:
    - everyone
