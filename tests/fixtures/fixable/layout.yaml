# The arrangement still places 'sw-gone', which no document declares (W138).
apiVersion: netviz.dev/v1alpha1
kind: layout
metadata:
  name: default
spec:
  views:
    l1:
      nodes:
        sw-a: {position: [54, 18]}
        sw-b: {position: [54, 126]}
        sw-gone: {position: [54, 234]}
