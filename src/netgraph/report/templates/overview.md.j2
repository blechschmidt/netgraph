{% import "macros.md.j2" as m %}
# {{ page.title | md }}

{{ page.summary | md }}

{{ m.provenance(meta) }}

{% for section in page.sections %}
{{ m.section(section, link) }}
{% endfor %}
---

Generated by [netgraph](https://github.com/blechschmidt/netgraph) {{ meta.version }} with
`netgraph report`. Every table on these pages comes from the same inventory as the
diagrams, so the two cannot disagree.
